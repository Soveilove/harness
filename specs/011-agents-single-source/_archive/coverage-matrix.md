# 覆盖矩阵

> 由 Sovei 阶段生成：scope
> Feature：011-agents-single-source

| 必需覆盖 | 证据 | 状态 |
|---|---|---|
| **入口/路由** | 修改入口为 `project init` 的 AGENTS.md 生成（`project.ts:182-223`）。 | 已覆盖 |
| **UI 状态** | CLI 输出新增提示指令；无 UI。 | 已覆盖 |
| **store/service** | `storage.write('AGENTS.md', ...)` 受存在性保护；无 store 结构变更。 | 已覆盖 |
| **参数** | `--force` 参数语义不变（force 仍覆盖）。 | 已覆盖 |
| **API** | `run` 逻辑内新增存在性分支；无 API 签名变化。 | 已覆盖 |
| **鉴权/计费** | 不涉及。 | N/A |
| **异步回调** | 无异步。 | N/A |
| **成功/失败/清理** | 成功：已存在→提示不覆盖；不存在→生成。失败：写入异常路径不变。 | 已覆盖 |
| **历史/详情/重试** | 已存在 AGENTS.md 不受破坏；重跑 init 不再覆盖。 | 已覆盖 |
| **兼容入口** | 新项目首次 init 正常；`--force` 覆盖；规则/配置生成不变。 | 已覆盖 |
| **测试/文档/运行时证据** | 需新增 project.test 断言"已存在时不覆盖并提示"；运行时用 tsx 验证。 | 已覆盖（1 处待加） |

## 明确不覆盖
- AGENTS.md 内容/结构
- 模板抽离
- 其他 init 逻辑

## 结论
影响面封闭：仅 `project.ts` 的 AGENTS.md 生成逻辑 + 新增测试断言。无未确定边界。

# 决策日志

> 由 Sovei 阶段生成：grill
> AI 代理：请依据下方提示契约，将本模板替换为真实内容。

## 决策摘要

### D1：唯一事实源

- 类型：范围性决策（已决）
- 决策：使用 `specs/<feature>/workflow-state.json` 作为唯一可读写事实源。
- 理由：R1.1 的目标是状态正确和直接校验；快照比事件回放更适合阶段游标、门禁和 revision 的原子更新。
- 拒绝方案：继续以 `workflow-events.jsonl` 为事实源并 replay。旧模型已产生伪 `learn`、重复 merge 和 YAML 旁路，保留它会把历史复杂度带入 v3。

### D2：审计方式

- 类型：可推断决策（已决）
- 决策：在 `workflow-state.json.history` 保存操作审计；history 不参与恢复、推导或阶段推进。
- 理由：保留追溯能力但避免第二事实源。
- 拒绝方案：独立事件日志作为恢复来源；不需要任意历史重放。

### D3：旧数据处理

- 类型：范围性决策（已决）
- 决策：不迁移、不回放、不兼容 v2；旧 Feature、事件、YAML 和 fixture 归档或删除后只支持 v3 schema。
- 理由：用户已确认历史数据可废弃，破坏性重构比兼容补丁更可靠。
- 拒绝方案：增加 v2 parser/migration/replay fallback；会保留已确认的错误语义。

### D4：状态更新并发

- 类型：可推断决策（已决）
- 决策：同目录临时文件写入后 rename；读改写由 `StorageBackend.withLock` 保护，并以 revision/CAS 拒绝陈旧写入。
- 理由：Windows 环境下避免半写文件和后写覆盖先写。
- 拒绝方案：直接覆盖写入或仅依赖内存锁；无法防止崩溃和跨进程竞争。

### D5：阶段转移

- 类型：可推断决策（已决）
- 决策：只允许从当前阶段完成；重复完成、越级、未知 schema、损坏 JSON 均明确失败，失败不改文件。
- 理由：阶段状态必须是可验证协议，不再静默补齐。
- 拒绝方案：允许前向跳跃并自动补齐；这是现有状态漂移的来源之一。

### D6：本 Feature 边界

- 类型：范围性决策（已决）
- 决策：R1.1 只交付状态核心；不实现 split 聚合、不实现 Explore→Quick、不改 Quick 执行语义。
- 理由：三个问题各自独立，混在一起会扩大验证面并制造半成品。
- 拒绝方案：一次性完成完整 R1；违反单需求串行开发约定。

## 多轮拷问记录

1. 若没有事件回放，损坏状态如何恢复？答：拒绝读取并明确报错，由人工重新 bootstrap；不从旧数据猜测。
2. 若 JSON 是事实源，Markdown 是否还能驱动流程？答：不能；Markdown 仅是人类和阶段产物投影。
3. 若两个进程同时更新怎么办？答：锁内读取最新 revision，CAS 检查后原子提交；陈旧调用失败。
4. 若重复 `complete` 是否幂等？答：不是；重复完成是协议错误，必须失败，避免隐藏调用方 bug。
5. 若旧 CLI/fixture 仍出现怎么办？答：直接重写/删除，不加入兼容分支。
6. 若 R1.1 触及 split 代码怎么办？答：只移除其对旧存储的依赖，保留后续 R1.2 所需接口，不实现新的聚合行为。

## 未决项

无。

---

## 提示契约

## 权威规则

当前 revision：0。仅当前顶层 Feature 产物具有权威性；history/ 下的文件是已失效证据，不得视为当前需求。

# 阶段：grill

## 输入
有效的 explore 结果（exploration.md）和当前请求。

## 操作
沿决策树逐层推进，依次解决依赖关系。对每个待决议项，先判断其类型，再选择处理方式：

1. 事实核实——能从代码、文档、配置或既有知识查证的，直接查证并记录结论，不问用户。
2. 可推断决策——项目约定、既有模式或证据指向明确最优解的，自行决策并记录理由和被拒绝方案，不等用户确认。
3. 范围性决策——无法从证据推断且会实质改变 Feature 范围的，逐个向用户提问。每个问题附上推荐答案和理由，一次只问一个，收到回复后再继续下一项；同时抛出多个问题会造成信息过载。

提问纪律：
- 先给出推荐答案和理由，再请用户确认或修正。
- 一次只提一个问题，等用户回复后再推进下一项。
- 用户确认视为达成共识；未确认的标注为未决。

## 输出
decision-log.md，包含每项决议的类型（事实核实/可推断决策/范围性决策）、决策内容、理由、被拒绝方案、状态（已决/未决）和未决项清单。

## 停止条件
所有事实已核实、所有可推断决策已记录、所有范围性决策已获得用户回复或标注为未决。本阶段不得实施。


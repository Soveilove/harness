# 2026-08-11

## 修正 AGENTS.md / MEMORY.md 快速通道表述

用户指出之前 AI 回复的逻辑错误：把 quick 通道和完整 Sovei 工作流当成必须叠加的层级，实际上两者是**二选一的互斥分流**（Feature 020 设计）：
- 低风险、明确、局部修改 → `sovei quick`（六步轻量闭环）
- 高风险/不确定/跨模块 → 完整工作流（12 阶段，代码在 implement 阶段完成）

已修正：
- `AGENTS.md` 两段 Quick Channel 章节改为明确区分两种场景，去掉"每次代码变更必须走快速通道"的绝对表述
- `MEMORY.md` 第 41 行同步修正
- **CLI 源码同步修正**：`packages/sovei-core/src/adapters/registry.ts` 中 4 个适配器（codex/claude/codebuddy/trae）的 `quickChannelDirective` 文本 + JSDoc 注释；`packages/sovei-core/src/cli/commands/project.ts` 中 `project init` 生成 AGENTS.md 的模板文本。tsc 通过，179/179 测试通过。

## DEV_BACKLOG.md 全面重整

用户提出 spec 治理新思路："拆机器层（git 内结构化数据）+ 人层（生成 docs + 每阶段蒸馏）"。经多角度讨论后判断：
- **方向对但对当前规模过度工程**——大部分阶段产出本身已是人可读摘要，再蒸馏信噪比低；docs 不放 git 会引入"clone 后看不到"的新问题
- **先用两个小 Feature 解决 80% 不适感**：P1-1 `sovei feature archive <id>`（过程产物折叠到 _archive/）+ P1-2 `sovei feature summary <id>`（生成聚合人可读视图）
- 观察使用体验后再决定是否需要架构级拆分

已全面重写 `DEV_BACKLOG.md`：
- 已完成项压缩为精简表格（不再展开分析）
- 未完成项按 P1/P2/P3 重新排列，编号统一（P1-1~P1-4, P2-1~P2-10, S-1~S-5, L-1~L-2, WP-1~WP-7）
- 场景一旧 P1（spec git 分层 + 归档 + 知识阈值）拆解：归档→P1-1, 分层→不需要（archive 替代）, 知识阈值→P1-4
- 待决策项重新编号 D1~D11
- 合并旧 P2-3（读更多文件）与 WP-1（按需读不全量内联）为 P2-6 统一设计
- 战略级缺口（问题三/四）保留存档但不占主清单篇幅

## P1-3 README 版本同步修复（快速通道）

- 版本号已是 2.5.10（package.json / README / npm latest 三方同步），发布脚本 `release-sovei.ps1` L65-76 已自动同步 README 版本号，一劳永逸
- 命令速查表补充 5 条新命令：`context cross-feature-index`、`context expand`、`quick`、`workspace preflight`、`adapters install/list`

## Feature 026-feature-archive 完成（完整 12 阶段工作流）

- **命令**：`sovei feature archive <id>` — 将已完成 Feature 的过程产物折叠到 `_archive/` 子目录
- **新建文件**：`cli/commands/feature.ts`（核心逻辑 + CLI 注册）、`test/feature-archive.test.mjs`（7 个测试）
- **修改文件**：`cli/index.ts`（+2 行 import + register）
- **设计要点**：排除法归档（白名单排除持久文件，其余 .md 全归档），幂等（_archive/ 已有同名跳过），状态检查（只归档 completed）
- **持久文件白名单**：decision-log.md / sync-report.md / load-summary.md / wayfinder.md（+ 非 .md 文件全保留）
- **测试**：186/186 通过（179 原有 + 7 新增，零回归）
- **知识提取**：3 个新观察入库（排除法优于包含法 / storage.list 非递归是天然隔离 / exists 只查文件不查目录）
- **DEV_BACKLOG P1-1 标记完成**


<!-- PROJECT-SPECIFIC: 本文件内容为项目专属(pino-front)，换项目时需清空重填。见 project.yaml -->
# 中枢工作记忆

- `E:\memory\harness` 是当前稳定发布源。
- Sovei Workflow 0.1.0 已启用 `load/grill/spec/scope/plan`；设计中的后续阶段仍不得视为已生效。
- A/B/C 的同步、保护边界和晋级流程只以 `E:\memory\SYNC.md` 为准。
- 历史讨论、试点日志和工程实例状态不自动注入；需要时从 Git 历史按证据查找。

# 2026-08-09

## Feature 020 quick-context-governance 核查与修复

- 用户问 020 快速通道是否完成。核查发现：代码主体已提交（commit `0fed844`，2026-08-07），但存在两个导致 `check` 必然失败的 bug：
  1. `packages/sovei-core/src/quick/usage.ts`：`UsageBaseSchema` 定义缺少 `z.object({` 开头（第 41 行后直接写 schema 字段），语法错误 TS1005。
  2. `packages/sovei-core/src/engine/workflow-engine.ts`：`prepareStage` 调用了未定义的 `recordContextObservation` 方法（TS2339），该方法未实现。
- 修复：补齐 `UsageBaseSchema` 声明；在 workflow-engine.ts `listStages()` 后实现 `recordContextObservation`（构建 context pack → 运行 shadow policy → 通过 UsageRecorder 记录 context-selected 观测，全程 try/catch 静默，不阻断 prepareStage）。用 `artifacts.list()+read()` 组合（无 listContent 方法），`WorkflowState` 无 baselineRevision 字段故传 null。
- 修复后：`pnpm check` 通过；`pnpm test` 121 tests / 0 failures（与 change-manifest 声明一致，但修复前从未真实通过）。020 相关 12 个测试（quick-cli/usage-git/context-policy/quick-contract）全绿。
- **结论**：020 实施代码已完整且测试全绿，但工作流仍停在 implement 阶段（workflow-state.yaml currentStage=implement；events 最后为 STAGE_PREPARED implement rev15，无 STAGE_COMPLETE implement）。`completedTaskIds` 为空，6 个任务 `[x]` 仅是 tasks.md 手写标记，未通过 `completeTask` 逐个记录。需调 completeTask(TASK-001..006) + completeStage('implement') 才能真正收尾。
- change-manifest.md 声称 "check passed / 121 tests" 在提交时并不真实（提交时 check 必失败）。

## Feature 020 工作流完整收尾（同日续）

- 修复第三个 bug：`readTaskIds` 正则 `/^\s*[-*]\s+\[[ xX]\]\s+([A-Za-z0-9][\w.-]*)\b/` 会把 skill 注入的"提示契约"模板里的 `- [ ] Acceptance criterion 1` 误判为任务 ID，导致 completeStage('implement') 报 "unfinished tasks: Acceptance, Criterion"。改为只匹配 `TASK-[\w.-]+` 前缀（项目契约明确规定 `- [ ] TASK-001: 描述` 格式，抽查 014/019 等均用 TASK- 前缀）。重新 build + 121 tests 全绿。
- change-manifest.md 重写为按 TASK-001~006 分节，使每项都被完整引用（completeTask 校验 change-manifest.md 文本须含任务 ID）。
- 用 CLI 顺序完成：`workflow implement ... --task TASK-00X --complete`×6 → `workflow implement --complete`（推进到 converge）。
- 产出收敛/验证/学习/同步产物：convergence-report.md、evidence.md、learning-report.md（含 yaml:knowledge-delta 3 条 candidate，知识对账 "3 新增 0 晋级" 写入 harness/project/knowledge/rule.json + architecture.json）、sync-report.md。
- verify 后有强制确认门（verify-confirmation，product + tech 双重签字）：用 `workflow confirm 020-quick-context-governance --stage verify --role product|tech --by user --reference evidence.md` 通过。
- 最终 workflow-state.yaml：status=completed，12 阶段全完成，completedTaskIds=[TASK-001..006]。
- **`sovei quick` CLI 实测可运行**：未声明范围→escalated；声明路径但越界→escalated（越界保护）。usage.jsonl 完整记录 run-start→context-selected→run-end 事件链（unknown token 语义正确）。
- 工作区改动：packages 下 usage.ts / workflow-engine.ts（bug 修复）+ specs 020 产物 + harness/project/knowledge/*.json（知识对账）均未提交，等待用户审阅/提交。
- **知识库新增 2 条**：rule "低风险局部修改走快速通道，高风险/不确定/越界升级完整 Sovei"、architecture "Context Policy 用影子变体先测量，实际上下文保持 full 不预切"、rule "usage 只记录脱敏事实，缺失 token 记 unknown 而非 0"。

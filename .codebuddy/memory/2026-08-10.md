# 2026-08-10

## DEV_BACKLOG 新增 P1-3：上下文包膨胀（Context Pack Bloat）

- 快速通道（S0/Quick）开发过程中发现：`sovei context build` 产出的上下文包随 Feature 积累单调增长，最终会撑爆 Agent 上下文窗口。
- 核对源码发现 5 个根因：required 项无整体预算上限、cross-feature 全量加载所有 Feature decision-log、shadow policy（scoped/index+on-demand）已实现但 `actual: 'full'` 未激活、suggested 仅条数限制无字符上限、Feature 产物无聚合上限。
- 建议方案分 4 步：①激活 scoped 变体为实际交付模式（核心）②引入字符预算+优先级截断 ③cross-feature 相关性过滤（Top-N 或 on-demand expand）④Feature 产物聚合上限。
- 已写入 DEV_BACKLOG.md §2.2（P1-3 摘要行）+ §2.3（详细描述）+ §6（建议开发顺序第 3 位，与 S0 快速通道直接相关）+ §7（待人工决策：激活时机）。

## DEV_BACKLOG 全面补充（第二轮）

- 全面扫描设计文档、specs learning-report、源码后，补充以下未记录的待开发项：
  - **§2.5 战略级能力缺口**：问题三（Drift Detection）完全未实现；问题四（统一关系模型/Graph Coding）完全未实现，距最终 Graph Coding 还差 5 层（统一关系模型/正向影响分析/反向同步/图查询上下文，语义合并预检已由 preflight 实现）。建议路径：drift MVP → 统一关系模型 → 影响分析引擎。
  - **§2.6 外部 Skills 后续**：多 binding 支持（一阶段多 skill）、Skill 附加文件解析（vendor skill 的 CONTEXT-FORMAT.md/ADR-FORMAT.md 被忽略）。
  - **§2.7 Quick 通道后续**：U3 usage export --redacted、U4 context build --paths 语义修正（与 P1-3 相关）、U5 阶段上下文预算值（等评测数据）。
  - **§2.8 Feature 流程状态遗留**：4 个 Feature 卡在 in_progress（002/009/012/015-learn-reconcile-verify），部分已被后续 Feature 替代，需人工确认归档。
  - **§6 IDE 适配器与发布生命周期**：Cursor Adapter（Phase 3 剩余）、Phase 4（harness-release.yaml/vendor lock/上游 diff/安装器评估）、Phase 2 回放验证。
- 更新 §0 状态快照：增加快速通道和 preflight 状态行、战略级缺口行。
- 更新 §7 建议开发顺序：新增 drift detection MVP（第 4 位）和统一关系模型（第 6 位）。
- 更新 §8 待人工决策：新增 drift detection 时机、统一关系模型设计范围、paths 语义修正合并等决策项。
- 更新已决清单：补充问题一/问题二的解决状态。

## Merge preflight 已实现确认

- `packages/sovei-core/src/preflight/` 目录完整实现（checker.ts + types.ts + index.ts），非 stub。
- CLI 命令 `sovei workspace preflight <source> <target>` 已接入 workspace.ts。
- 三类语义冲突检测：红线冲突（blocking/warning）、知识冲突（blocking/warning）、coverage 冲突（warning）。
- 四种裁决动作：merge/isolate/override/manual，裁决后重新评估 canMerge。
- 报告写入 `preflight-events.jsonl`，CLI 支持 `--feature` 和 `--report` 选项。
- 已从 index.ts 导出，测试 135/135 通过。未发布到 npm。


## P0-2 Merge Preflight 语义冲突预检已实现

- 新增 `packages/sovei-core/src/preflight/` 模块：`types.ts`（冲突类型/裁决/报告结构）、`checker.ts`（`MergePreflightChecker` 核心检测器 + `renderPreflightReport` Markdown 渲染）、`index.ts`（barrel export）。
- 三类语义冲突检测：
  1. **红线冲突**：同一 redline ID 规则不同→blocking；执行级别不同→blocking；active/inactive 差异→warning。
  2. **知识冲突**：同一 ID content 不同→blocking；同类型同标题不同 ID 不同内容（语义冲突）→warning；lifecycle 差异→warning。
  3. **coverage 冲突**：同一代码表面在 coverage-matrix.md 中变更意图不同→warning（解析表格行和列表行）。
- 冲突裁决：`resolveConflict()` 支持 merge/isolate/override/manual 四种动作，裁决后重新评估 `canMerge`（blocking 且非 manual 裁决解除后可合并）。
- 事件流：`persistReport()` 将 PREFLIGHT_RUN 事件写入两侧工作区的 `harness/project/governance/preflight-events.jsonl`（append-only，审计一致）。
- CLI 命令：`sovei workspace preflight <source> <target>`，选项 `--feature <path>`（可重复，指定 coverage-matrix 检查路径）、`--report <file>`（输出 Markdown 报告文件）。
- 测试 135/135 通过（新增 11 条 preflight 测试：无冲突通过、红线规则/级别/状态冲突、知识内容/语义冲突、coverage 冲突、事件流写入、裁决后重评、报告渲染、空数据）。
- DEV_BACKLOG.md 已更新：P0-2 标记 ✅ 已实现，测试基线 135/135，建议开发顺序第 2 位标记完成。
- **场景二两项 P0 均已落地**（P0-1 红线 branch 隔离 + P0-2 merge preflight）。

## Feature 021-quick-json-slim 已完成（完整 12 阶段工作流）

- **问题**：`sovei quick --json` 输出 117KB，宿主 Agent 解析困难。根因：`QuickEvaluationResult.policy` 直接引用完整 `ContextPolicyResult`，其中 `shadow.full`/`shadow.scoped`/`shadow.indexOnDemand` 三变体内联了所有 `ContextItem` 的完整 `content`（每条最多 4000 字符 × 30+ 条目 × 3 变体）。
- **修复**：在 `quick/run.ts` 新增 `QuickPolicySummary` 接口 + `summarizePolicy()` 函数，`QuickEvaluationResult.policy` 类型从完整 `ContextPolicyResult` 改为 `QuickPolicySummary`（仅 controlPlane + shadowSummaries + index）。内部评估逻辑不变（仍用完整 policy 做 needsEscalation 判定），仅返回时精简。
- **效果**：输出从 117KB 降至 37KB（68% 缩减）。剩余体积主要来自 `policy.index`（35 条 × 240 字符 summary ≈ 15KB）+ `shadowSummaries.*.ids`，属 P1-3 上下文包膨胀治理范畴。
- **测试**：137/137 通过（原 135 + 新增 2 条 `quick-policy-slim.test.mjs`）。
- **知识提取**：新增 candidate 知识条目 `rule-内部评估类型与输出序列化类型应在边界分离`（当函数返回包含大型内部数据结构的评估结果时，应在序列化边界提供精简摘要类型）。
- **DEV_BACKLOG 关联**：P1-3（§2.3）已记录通用上下文包膨胀问题，但 `quick --json` 输出膨胀这个具体症状未单独记录。本次修复是 P1-3 的一个子集。

## DEV_BACKLOG 同步 + 子 Agent 强化方向记录

### DEV_BACKLOG 同步内容
- 测试基线 135 → 137（Feature 021 新增 2 条 `quick-policy-slim.test.mjs`）。
- §0 状态快照：最新 Feature 增加 021-quick-json-slim（completed）；快速通道（S0）描述补充 Feature 021 的 JSON 输出精简（117KB → 37KB）。
- §2.7 Quick 通道后续项：新增 Feature 021 已解决项（`quick --json` 输出膨胀），标记 ✅。
- §2.9 新增「子 Agent 强化方向（架构观察）」：6 个候选节点（SA-1 ~ SA-6），按优先级排列。
- §7 建议开发顺序：新增第 10 位「子 Agent 强化」。
- §7 待人工决策：新增子 Agent 强化决策项。

### 子 Agent 强化方向（§2.9，架构观察）
思考 Sovei 工作流中哪些串行 I/O 密集型节点可用子 Agent（sub-agent）并行化：

1. **SA-1 cross-feature 相关性过滤**（P1）：`--cross-feature` 串行遍历所有 Feature 目录读 decision-log，50+ Feature 时瓶颈明显。子 Agent 并行读取 → 各自输出摘要 + 相关性评分 → 主 Agent 取 Top-N。直接解决 P1-3 上下文包膨胀。**优先实施**。
2. **SA-2 context build 组装**（P2）：required 项（红线+知识+规范+产物）串行加载。子 Agent 分组并行加载，主 Agent 合并 + 优先级截断。
3. **SA-3 converge/verify 代码审查**（P2）：单一 Agent 会话做全部审查。子 Agent 分维度并行（类型安全/测试覆盖/规则合规/架构合规），主 Agent 汇总裁决。
4. **SA-4 drift detection 影响面评估**（P1，与 drift MVP 同期）：未实现。子 Agent 按规则类别并行扫描（红线 scope/coverage-matrix/knowledge 证据），主 Agent 汇总 drift 报告。
5. **SA-5 preflight 三类冲突检测**（P2）：当前串行执行红线→知识→coverage，三类互相独立可并行。当前数据量小收益有限，规模化后可提升。
6. **SA-6 learn 知识提取**（P2）：串行解析→匹配→创建/晋级。子 Agent 并行（提取新观察/搜索匹配/评估蒸馏价值），知识库写入仍需串行保一致性。

**设计约束**：子 Agent 强化是宿主 AI 层面的并行编排，不是 Sovei CLI 自身并发改造。需定义子 Agent 契约（输入 JSON + 输出 JSON），类似 `sovei quick --json` 的模式。

## DEV_BACKLOG.md 重新整理

- 用户反馈"有点看不懂，哪些完成了哪些没完成"，重新组织了整个文件结构。
- **新增 §1 全局总览**：一张「✅ 已完成项」表（9 项）+ 一张「❌ 未完成项」表（按优先级排列，23 项），让完成/未完成一目了然。
- **详情分区重编号**：原 §2.x → §3.x，原 §3/4/5/6/7/7 → §4/5/6/7/8/9。每个表格项都加了 ✅/❌/⚠️ 状态标记。
- **§8 建议开发顺序**改为表格，步骤 1-2 标 ✅，步骤 3-10 标 ❌ 待开发/待评估。
- **§9 待人工决策**改为表格，已决项标 ✅，未决项标 ❓。
